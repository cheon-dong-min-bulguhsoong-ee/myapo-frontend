# S-04 — 내 증명서

홈 "내 증명서" 진입. 보유 credential 카드 리스트.

## Layout

```
┌──────────────────────┐
│ ←  내 증명서           │ app-bar 56px
├──────────────────────┤
│ ┌──────────────────┐ │ segmented (사용 가능 / 만료됨)
│ │ 사용 가능 │ 만료됨   │ │ 좌 기본, edge 20px, height 44px
│ └──────────────────┘ │
│ ┌──────────────────┐ │ card paper / hairline / shadow
│ │ 납세증명서 (영문)  │ │ headline 17 / 700
│ │  ↑ 한국인 예시       │ │ 외국인이면 "Tax Payment Cert."만
│ │ KR-NTS            │ │ pill-neutral (mono 13)
│ │ 발급일  2026-04-12 │ │ caption 13
│ │ 유효  2026-07-12 D-3│ │ + pill-warning (D-7 이내)
│ └──────────────────┘ │
│ ┌──────────────────┐ │ 다음 카드, gap 16px
│ │ ...               │ │
│ └──────────────────┘ │
└──────────────────────┘ (스크롤)
```

## 동작

- 사용 가능 탭: 만료 전 credential
- 만료됨 탭: 만료 후 credential (회색 톤, "만료일" 라벨)
- 카드 액션 버튼 **없음** — 카드 전체 tap → S-04.detail
- D-7 이내 만료: `pill-warning` "D-N" 표시

## 빈 상태

큰 outline 아이콘(`wallet` 64px ink-muted) + "발급받은 증명서가 없어요" + `btn-primary` "증명서 발급하러 가기" → A-01

만료됨 탭 빈 상태: "만료된 증명서가 없어요" (CTA 없음)
